name: call_center_app
description: Call Center AI Assistant mobile client
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.0
  speech_to_text: ^7.0.0
  audioplayers: ^6.0.0
  flutter_markdown: ^0.7.4
  markdown: ^7.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
